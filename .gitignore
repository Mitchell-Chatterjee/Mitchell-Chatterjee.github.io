# Hugo build output (keep caches ignored but allow tracking public/)
hugo_stats.json
.hugo_build.lock

# Hugo cache / resources
resources/
.hugo/

# Generated assets (placeholders that shouldn't be in repo)
assets/media/og-default.svg
assets/media/og-ecg.svg

# OS/editor files
.vscode/
*.swp
*~
.DS_Store
